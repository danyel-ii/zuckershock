{
  "name": "whack-a-mole-kid-safe",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/dev-server.mjs --root dist --port 4173",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

